BEGIN;

{{#each entities}}
CREATE TABLE {{entity.tableName}} (
	id BIGSERIAL PRIMARY KEY,
{{#each nonIdFields}}
	{{dbTag}} {{sqlType}}{{#unless @last}},{{/unless}}{{#if @last}}{{/if}}
{{/each}}
);

{{/each}}
COMMIT;
