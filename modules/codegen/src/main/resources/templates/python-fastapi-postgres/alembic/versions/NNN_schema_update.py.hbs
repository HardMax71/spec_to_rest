"""Schema update for {{service.name}}.

Revision ID: {{migration.revision}}
Revises: {{migration.downRevision}}
Create Date: {{migration.createdDate}}
"""
from collections.abc import Sequence

from alembic import op
import sqlalchemy as sa
{{#if migration.needsPostgresDialect}}
from sqlalchemy.dialects import postgresql
{{/if}}


revision: str = "{{migration.revision}}"
down_revision: str | None = "{{migration.downRevision}}"
branch_labels: str | Sequence[str] | None = None
depends_on: str | Sequence[str] | None = None


def upgrade() -> None:
{{#each migration.upgradeStatements}}
    {{{this}}}
{{else}}
    pass
{{/each}}


def downgrade() -> None:
{{#each migration.downgradeStatements}}
    {{{this}}}
{{else}}
    pass
{{/each}}
