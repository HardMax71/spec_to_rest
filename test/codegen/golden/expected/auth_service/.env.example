# Database connection string (async SQLAlchemy + asyncpg)
DATABASE_URL=postgresql+asyncpg://auth_service:auth_service@db:5432/auth_service

# Base URL the service is reachable at (used for constructing absolute URLs)
BASE_URL=http://localhost:8000

# Logging level: debug, info, warning, error
LOG_LEVEL=info
