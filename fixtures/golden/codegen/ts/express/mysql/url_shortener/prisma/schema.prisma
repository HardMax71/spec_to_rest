generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mysql"
  url      = env("DATABASE_URL")
}

model UrlMapping {
  id BigInt @id @default(autoincrement())
  code String
  url String
  created_at DateTime
  click_count Int

  @@map("url_mappings")
}

