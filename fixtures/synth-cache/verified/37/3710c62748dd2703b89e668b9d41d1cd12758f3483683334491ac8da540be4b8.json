{
  "candidate" : "method Resolve(st: ServiceState, code: ShortCode) returns (url: LongURL)\n  requires ServiceStateInv(st)\n  requires ShortCodeWhere(code)\n  requires code in st.store\n  ensures (code in old(st.store) && url == old(st.store)[code])\n  ensures st.store == old(st.store)\n  ensures (code in st.metadata && (code in old(st.metadata) && st.metadata[code].click_count == old(st.metadata)[code].click_count + 1))\n  ensures ServiceStateInv(st)\n  modifies st\n{\n  // Capture current metadata locally (this is the pre-state metadata)\n  var meta := st.metadata;\n\n  // From the precondition and ServiceStateInv we know code is also in metadata.\n  // Access the current mapping and URL.\n  var m := meta[code];\n  url := st.store[code];\n\n  // Increment the click_count for the given code, leaving other metadata entries unchanged.\n  st.metadata := meta[code := UrlMapping(m.code, m.url, m.created_at, m.click_count + 1)];\n\n  // Quick sanity assertions to help the verifier relate pre- and post-state facts.\n  assert url == old(st.store)[code];\n  assert st.store == old(st.store);\n\n  // The metadata for `code` was updated to have click_count + 1.\n  assert st.metadata[code].click_count == m.click_count + 1;\n  // And m was the old metadata for that code, so this establishes the required postcondition.\n  assert st.metadata[code].click_count == old(st.metadata)[code].click_count + 1;\n\n  // Finally, re-establish the service invariant.\n  // - store URIs unchanged from pre-state, so still valid\n  // - keys of metadata unchanged (we updated an existing key)\n  // - all click_count values are >= 0 (old values were >=0 and we incremented one of them)\n  assert (forall c :: c in st.store ==> isValidURI(st.store[c]));\n  assert st.store.Keys == st.metadata.Keys;\n  assert (forall c :: c in st.metadata ==> st.metadata[c].click_count >= 0);\n  assert ServiceStateInv(st);\n}",
  "body" : "  // Capture current metadata locally (this is the pre-state metadata)\n  var meta := st.metadata;\n\n  // From the precondition and ServiceStateInv we know code is also in metadata.\n  // Access the current mapping and URL.\n  var m := meta[code];\n  url := st.store[code];\n\n  // Increment the click_count for the given code, leaving other metadata entries unchanged.\n  st.metadata := meta[code := UrlMapping(m.code, m.url, m.created_at, m.click_count + 1)];\n\n  // Quick sanity assertions to help the verifier relate pre- and post-state facts.\n  assert url == old(st.store)[code];\n  assert st.store == old(st.store);\n\n  // The metadata for `code` was updated to have click_count + 1.\n  assert st.metadata[code].click_count == m.click_count + 1;\n  // And m was the old metadata for that code, so this establishes the required postcondition.\n  assert st.metadata[code].click_count == old(st.metadata)[code].click_count + 1;\n\n  // Finally, re-establish the service invariant.\n  // - store URIs unchanged from pre-state, so still valid\n  // - keys of metadata unchanged (we updated an existing key)\n  // - all click_count values are >= 0 (old values were >=0 and we incremented one of them)\n  assert (forall c :: c in st.store ==> isValidURI(st.store[c]));\n  assert st.store.Keys == st.metadata.Keys;\n  assert (forall c :: c in st.metadata ==> st.metadata[c].click_count >= 0);\n  assert ServiceStateInv(st);",
  "usage" : {
    "inputTokens" : 1062,
    "outputTokens" : 5720
  },
  "model" : "gpt-5-mini-2025-08-07",
  "promptVersion" : "v1",
  "outcome" : "verified"
}
