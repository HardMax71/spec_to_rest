{
  "candidate" : "  // Hand-written body, dafny-verified against this skeleton; CEGIS models\n  // repeatedly failed the bounded-freshness construction (see PR notes).\n  var oldStore := st.store;\n  var oldMeta := st.metadata;\n\n  var maxLen := 0;\n  var keys := oldStore.Keys;\n  while keys != {}\n    invariant keys <= oldStore.Keys\n    invariant forall k :: k in oldStore.Keys && k !in keys ==> |k| <= maxLen\n    invariant maxLen >= 0\n    decreases |keys|\n  {\n    var k :| k in keys;\n    if |k| > maxLen {\n      maxLen := |k|;\n    }\n    keys := keys - {k};\n  }\n  assert forall k :: k in oldStore ==> |k| <= maxLen;\n\n  var target := if maxLen + 1 >= 6 then maxLen + 1 else 6;\n  var fresh_code := \"\";\n  while |fresh_code| < target\n    invariant |fresh_code| <= target\n    decreases target - |fresh_code|\n  {\n    fresh_code := fresh_code + \"x\";\n  }\n  code := fresh_code;\n  assert |code| == target;\n  assert |code| >= 6;\n  assert ShortCodeWhere(code);\n  assert |code| > maxLen;\n  assert code !in oldStore;\n\n  st.store := oldStore[code := url];\n  st.metadata := oldMeta[code := UrlMapping(code, url, 0, 0)];\n  short_url := st.base_url + \"/\" + code;\n\n  assert st.store.Keys == oldStore.Keys + {code};\n  assert |st.store.Keys| == |oldStore.Keys| + 1;\n",
  "body" : "  // Hand-written body, dafny-verified against this skeleton; CEGIS models\n  // repeatedly failed the bounded-freshness construction (see PR notes).\n  var oldStore := st.store;\n  var oldMeta := st.metadata;\n\n  var maxLen := 0;\n  var keys := oldStore.Keys;\n  while keys != {}\n    invariant keys <= oldStore.Keys\n    invariant forall k :: k in oldStore.Keys && k !in keys ==> |k| <= maxLen\n    invariant maxLen >= 0\n    decreases |keys|\n  {\n    var k :| k in keys;\n    if |k| > maxLen {\n      maxLen := |k|;\n    }\n    keys := keys - {k};\n  }\n  assert forall k :: k in oldStore ==> |k| <= maxLen;\n\n  var target := if maxLen + 1 >= 6 then maxLen + 1 else 6;\n  var fresh_code := \"\";\n  while |fresh_code| < target\n    invariant |fresh_code| <= target\n    decreases target - |fresh_code|\n  {\n    fresh_code := fresh_code + \"x\";\n  }\n  code := fresh_code;\n  assert |code| == target;\n  assert |code| >= 6;\n  assert ShortCodeWhere(code);\n  assert |code| > maxLen;\n  assert code !in oldStore;\n\n  st.store := oldStore[code := url];\n  st.metadata := oldMeta[code := UrlMapping(code, url, 0, 0)];\n  short_url := st.base_url + \"/\" + code;\n\n  assert st.store.Keys == oldStore.Keys + {code};\n  assert |st.store.Keys| == |oldStore.Keys| + 1;\n",
  "usage" : {
    "inputTokens" : 0,
    "outputTokens" : 0
  },
  "model" : "gpt-5-mini-2025-08-07",
  "promptVersion" : "v2",
  "outcome" : "verified"
}
