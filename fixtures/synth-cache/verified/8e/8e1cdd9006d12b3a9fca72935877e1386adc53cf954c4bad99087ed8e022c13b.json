{
  "candidate" : "results := [];\n  var keys := st.todos.Keys;\n  while keys != {}\n    decreases |keys|\n    invariant keys <= st.todos.Keys\n    invariant st.todos == old(st.todos)\n    invariant forall t :: (t in results) ==> ((((t in old(st.todos).Values && (status_filter == None || t.status == status_filter.value)) && (priority_filter == None || t.priority == priority_filter.value)) && (tag_filter == None || tag_filter.value in t.tags)))\n  {\n    var k :| k in keys;\n    keys := keys - {k};\n    var t := st.todos[k];\n    if (status_filter == None || t.status == status_filter.value)\n        && (priority_filter == None || t.priority == priority_filter.value)\n        && (tag_filter == None || tag_filter.value in t.tags) {\n      results := results + [t];\n    }\n  }",
  "body" : "results := [];\n  var keys := st.todos.Keys;\n  while keys != {}\n    decreases |keys|\n    invariant keys <= st.todos.Keys\n    invariant st.todos == old(st.todos)\n    invariant forall t :: (t in results) ==> ((((t in old(st.todos).Values && (status_filter == None || t.status == status_filter.value)) && (priority_filter == None || t.priority == priority_filter.value)) && (tag_filter == None || tag_filter.value in t.tags)))\n  {\n    var k :| k in keys;\n    keys := keys - {k};\n    var t := st.todos[k];\n    if (status_filter == None || t.status == status_filter.value)\n        && (priority_filter == None || t.priority == priority_filter.value)\n        && (tag_filter == None || tag_filter.value in t.tags) {\n      results := results + [t];\n    }\n  }",
  "usage" : {
    "inputTokens" : 0,
    "outputTokens" : 0
  },
  "model" : "gpt-5-mini-2025-08-07",
  "promptVersion" : "v2",
  "outcome" : "verified"
}
